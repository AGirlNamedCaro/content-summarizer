# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in content_summarizer.gemspec
gemspec

# Optional: Add gems for local development that aren't in gemspec
gem "irb"  # Useful for debugging, but not required for gem
